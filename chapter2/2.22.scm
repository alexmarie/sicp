This implementation is iterating through the the list and pushing each squared result on the front, resulting in a list reversed from the expected order.

Using cons with a list as the first parameter and an integer as the second parameter results in a nested list (1 2 3 4) -> ((((1) 4) 9) 16)
